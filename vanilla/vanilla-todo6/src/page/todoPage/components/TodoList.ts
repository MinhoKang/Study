import { Store } from "../../../store/store";
import { TodoPage } from "../../TodoPage";

export class TodoList {
  constructor(
    $app: HTMLElement,
    $main: HTMLElement,
    store: Store,
    TodoPage: TodoPage
  ) {}
}
